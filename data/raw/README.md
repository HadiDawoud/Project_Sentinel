# Raw data (local only)

Put your **labeled** source CSV here on your machine. This directory’s contents are **gitignored** except this README.

**Expected columns:** `text`, `label` (integers `0`–`3` by default).

See `data/README.md` for the full workflow and `data/examples/labeled_format_demo.csv` for a minimal shape example.
